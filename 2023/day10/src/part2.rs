pub fn run(input: &str) -> usize {
    // get loop
    let sketch = crate::common::parse(input).unwrap();
    let sloop = sketch.get_loop();

    // go through array row by row (outside R outside)
    // go through array collumn by collumn (outside I outside)

    0
}
